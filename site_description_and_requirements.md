# url:
* https://www.pythonlidar.com

# Site name:
* Python LiDAR & Point Cloud Workflows

# Audience: 
* LiDAR analysts, Python GIS devs, surveying tech teams, infrastructure/urban planning engineers

# site_description:
- Design and chain PDAL pipelines for point cloud processing
- Implement ground filtering, classification, and DTM/DSM generation
- Automate batch workflows and integrate with cloud infrastructure
- Validate outputs and troubleshoot pipeline failures
 
# Focus:
PDAL pipeline design, ground filtering, classification, DTM/DSM generation, batch automation, cloud integration, and QC/validation.

# build_stack:

* static site generator
* build with 11ty.js

# Frontpage:
* Should include a hero with buttons with CTA to the relevant section, the buttons should have colorful icons.
* 2-3 paragraphs of text.
* Links to relevant pages on the content pages.
* Hover effects.
* Responsive.
* Elegant styling.
* Links should be styled and have hover effects.
* Description of the site.
* Description of the content on the content pages.
* The site should be optimized for mobile and desktop with widescreen displays.

# content pages:
* all codeblocks should have a QA check for valid code and indentation.
* title should be styled and colorful
* headers should be styled and elegant.
* paragraphs should be styled and elegant.
* Codeblocks with syntax highlighting that matches the site's color scheme having a light background. There should be a copy button to copy the code to the clipboard.
* Breadcrumbs and navigation links to related content.
* click boxes should be togglable. [ ] should be rendered as checkboxes that can be checked or unchecked. a dot in front of the checkbox should be removed. Then the checkbox is checked, the text should have a line-through.
* inline code should be styled and have a light background that matches the site's color scheme and not take too much focus, they should not have a border and blend well with the surrounding text.
* scan the codeblocks for diagrams and other text that could be better styled with mermaid. Convert it so it will be rendered with mermaid and styled to match the site's color scheme.
* Hover effects.
* Responsive.
* Elegant styling.
* in page link should take into account the header height, so the content is not hidden behind the header when clicking on an in page link.
* tables should be responsive and scroll horizontally.
* tables should be styled.
* links should be styled. And have hover effects.
* The FAQ and Frequently Asked Question sections should be styled as accordions with the question in the header and the answer then you open the accordion.
* The site should be optimized for mobile and desktop with widescreen displays.
* The text should use the full width of its container on desktops, but not the full width of the page, and not be constrained to a narrow column, while still being responsive and elegant on mobile devices.

# links:
* the site should not include links to external sites, other than what is already in the content pages.

# main app header:
* custom-made logo that is colorful and elegant, and represents the site.
* Logo should also be used for favicon.ico and other icons modern browsers can use to represent the site.
* should include a link to the home page. Elegant styling and hover effects.
* should include a link to the content pages. Elegant styling and hover effects.
* It should use icons and text to indicate the current page.
* It should be responsive.
* It should have hover effects.
* It should be sticky to the top of the page.
* It should be visible on all pages.
* It should be optimized for mobile and desktop with widescreen displays.

# footer:
* should include a link to the main sections of the site. they should be at the bottom of the page, even on pages with no content.

# PWA:
* the site should be a PWA. It should have a manifest.json file that includes the site's name, icons, and other metadata. It should also have a service worker that caches the site's assets for offline use.
* The service worker should be optimized for performance and should use modern caching strategies to ensure fast loading times even when offline.
* The site should be optimized for mobile and desktop with widescreen displays.
* The PWA should work with Web App Manifest (Chromium world) Android, Desktop (Chrome, Edge, some Linux installs)
* The PWA should work with Apple meta tags (iOS) iPhone / iPad home screen
 
# icons:
* the site should have a favicon.ico and other icons modern browsers can use to represent the site as a full PWA.

# color scheme:
* the site should have a light color scheme inspired by the site content. The color scheme should be elegant and professional and should use colors that are easy on the eyes.

# Please widen the site on desktops. The site should not be constrained to a narrow column, but should take advantage of the available screen real estate on desktops, while still being responsive and elegant on mobile devices.