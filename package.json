{
  "name": "pythonlidar.com",
  "version": "1.0.0",
  "private": true,
  "description": "Python LiDAR & Point Cloud Workflows — static site (11ty).",
  "scripts": {
    "clean": "rm -rf _site",
    "build": "eleventy",
    "serve": "eleventy --serve --port=8080",
    "deploy": "npm run build && wrangler pages deploy _site --project-name=pythonlidar --branch=main --commit-dirty=true",
    "deploy:preview": "npm run build && wrangler pages deploy _site --project-name=pythonlidar --branch=preview --commit-dirty=true"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-navigation": "^0.3.5",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0",
    "markdown-it": "^14.1.0",
    "markdown-it-anchor": "^9.0.1",
    "markdown-it-attrs": "^4.3.1",
    "markdown-it-task-lists": "^2.1.1"
  }
}
